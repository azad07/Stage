{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "build everything",
			"command": "${workspaceFolder}/build-all.sh",
			"args": [],
			"options": {
				"cwd": "${fileDirname}"
			},
			"problemMatcher": [
				"$gcc"
			],
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"detail": "task to build everything."
		}
	]
}